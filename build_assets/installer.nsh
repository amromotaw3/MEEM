!macro customHeader
  # This macro is called by electron-builder
!macroend

!macro customInit
  # Custom initialization
!macroend

# Customize colors (Modern UI 2)
!define MUI_BGCOLOR "12121C"
!define MUI_TEXTCOLOR "FFFFFF"

# We can also use WinAPI to set the window background if needed, 
# but defining the MUI colors is the standard way.
